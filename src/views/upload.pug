extends base.pug

block content
    form(method="post")
        label(for=`upload_video_title`) title
        input(id=`upload_video_title`, name="title", placeholder="Title", required, type="text")
        input(value="Upload Video", type="submit")